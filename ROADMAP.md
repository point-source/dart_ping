# Roadmap

No outstanding workstreams.

The maintenance & modernization refresh (`§spec:dependency-currency` …
`§spec:code-audit`) is complete and merged, as is the #73 SPM work.
Sections are removed from this file as they finish — presence here means
work is not yet done.

Deferred follow-ups that are not yet scheduled are tracked as GitHub
issues rather than here (e.g. the `base_ping` stream/process lifecycle
edge cases surfaced by the code audit).
