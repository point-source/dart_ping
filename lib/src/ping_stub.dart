import 'package:dart_ping/src/dart_ping_base.dart';

Ping getPing(
        String host, int count, double interval, double timeout, bool ipv6) =>
    throw UnsupportedError('Ping not supported on this platform');
